Во время тестов были найдены следующие баги:

result.Users = data[0:len(data)] - вообще лишнее, не баг, но выглядит не очень, можно распаковать сразу в result.Users

const (
	orderAsc  = iota
	orderDesc
) - оюъявляется ниже

errTest = errors.New("testing") - тоже не надо, ошибка ни о чем не говорит

if len(result.Users) > req.Limit {
    result.NextPage = true
} - всегда приходит величина равная req.Limit, а значит для того чтобы сделать переключатель надо высылать полный размер списка данных

клиент не проверяет offset > limit, но проверяет сервер

//нужно для получения следующей записи, на основе которой мы скажем - можно показать переключатель следующей страницы или нет
req.Limit++ - все равно надо присылать какую-то инфу с сервера чтобы это работало